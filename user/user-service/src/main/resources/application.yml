#db config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/easyry?useUnicode=true&characterEncoding=utf8
    username: easyry
    password: easyry
    type: com.alibaba.druid.pool.DruidDataSource

#dubbo config
dubbo:
  application:
    name: user-service
  protocol:
    name: dubbo
  registry:
    address: zookeeper://localhost:2181
server:
  port: ${random.int[1000,10000]}
